class Solution {
public:
    bool stoneGame(vector<int>& piles) {
        // alice forces bob to choose either only odd or even indexed elements (which has a smaller sum)
        return true;
    }
};
